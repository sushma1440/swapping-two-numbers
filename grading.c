#include<stdio.h>
int main()
{
  int mark;
  printf("Enter a number");
  scanf("%d, &mark);
if(mark>=85&&mark<=100)
        {
          printf("A grade");
        }
        else if(mark>=70&&mark<=84)
        {
          ptintf("B grade");
        }
          else if(mark>=55&&mark<=69)
          {
            printf("C grade");
          }
        else if(mark>=40&&mark<=54)
        {
          printf("D grade");
        }
        else if(below 40)
        {
          printf("F grade");
        }
        return 0;
        }
  
            
     
   
